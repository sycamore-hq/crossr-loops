## Specialization

This skill is the dedicated execution-loop orchestration specialization of the harness layer (precondition: `gan-verdict` active; language/domain + code GAN skills available to subagents — the conductor never loads them; preferably an AVRIL-blessed backlog). It supplies the blessed-intake gate, per-PBI PETC, stratified Pinto board I/O, AC evidence gate, and the iron “never touch code” boundary, while preserving every principle of the base (postcondition: combined output satisfies this contract plus the specialization with zero contradictions).
