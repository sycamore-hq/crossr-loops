[package]
name = "graph-runner"
version = "0.1.0"
edition = "2021"
rust-version = "1.94"
description = "Typed reader for crossr-loops graphs/*.json. Replays; never runs."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
