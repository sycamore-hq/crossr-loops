# Pins consumed by harness-bootstrap / sync-skills.
# Same contract as harness and skills — consumer pins, not a self-advertisement
# of this remote's published tag. Not a third tracker — features.json remains
# the work log. No git submodules.
skills = "v1-gan-layers"
loops  = "v1-cards"
